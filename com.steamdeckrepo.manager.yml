id: com.steamdeckrepo.manager
sdk: org.gnome.Sdk
runtime: org.gnome.Platform
runtime-version: "43"
rename-icon: repomanager
rename-desktop-file: repomanager.desktop
command: runner.sh
finish-args:
  - --socket=fallback-x11
  - --socket=wayland
  - --socket=pulseaudio
  - --share=network
  - --filesystem=home # Seems to fix issues when enabling this flag with flatseal
  - --filesystem=~/.steam:rw # Steam (Non-flatpak)
modules:
  - python3-requirements.json
  - name: runner
    sources:
      - type: git
        url: https://github.com/CapitaineJSparrow/steam-repo-manager
        commit: dffd5b991d62fa8f666967973c106011b69077af
    buildsystem: simple
    build-commands:
      - cp -r . /app
      - install -D ./tools/runner.sh /app/bin/runner.sh
      - install -Dm644 ./flatpak/com.steamdeckrepo.manager.appdata.xml /app/share/metainfo/com.steamdeckrepo.manager.appdata.xml
      - install -Dm644 ./flatpak/icon_32.png -t /app/share/icons/hicolor/32x32/apps
      - install -Dm644 ./flatpak/icon_64.png -t /app/share/icons/hicolor/64x64/apps
      - install -Dm644 ./flatpak/icon_128.png -t /app/share/icons/hicolor/128x128/apps
      - install -Dm644 ./flatpak/icon_256.png -t /app/share/icons/hicolor/256x256/apps
      - install -Dm755 ./flatpak/repomanager.desktop -t ${FLATPAK_DEST}/share/applications/
